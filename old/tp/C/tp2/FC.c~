#include <stdio.h>
#include <stdlib.h>

float celsius (float f)

{
  float c;
  
    c = ((f-32.0) * (5.0/9.0));
   
   
   
   return c;
}

int main () 

{
  int i;
  float a;
  printf("fahrenheit   Celsius\n");
  
   for(i=0;i<=300;i=i+20) 
    {
      a=celsius(i);
      printf(" %d       %.2f\n", i,a);
      
     }
  return EXIT_SUCCESS;
}


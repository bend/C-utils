#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main (int argc, char*argv[])
{
  char car;
  int i ;
  
 

  for (i=0; i<strlen(argv[1]);i++)
  {
      car=argv[1][i] ;
    
      if (car>='a' && car<='z')
      {
        car=car ^ 0x20;
        printf("%c",car);
        
      }
      else printf("%c",car);
   
   }
   printf("\n");
return EXIT_SUCCESS;
}
